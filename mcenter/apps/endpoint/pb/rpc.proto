syntax = "proto3";

package devcloud.mcenter.endpoint;
option go_package = "github.com/hezihua/devplat/mcenter/apps/endpoint";

import "apps/endpoint/pb/endpoint.proto";

service RPC {
  // //查询用户列表
  rpc RegistryEndpoint(RegistryRequest) returns(EndpointSet);

}